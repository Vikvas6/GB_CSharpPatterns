using Asteroids.Object_Pool;
using UnityEngine;


namespace Asteroids
{
    internal class GameStarter : MonoBehaviour
    {
        private void Start()
        {
            //Task 1
            //a
            //var enemy = Enemy.CreateBattleshipEnemy(new Health(100.0f, 100.0f));
            //enemy.transform.position += Vector3.up * 3;
            //b
            //IEnemyFactory battleShipFactory = new EnemyBattleshipFactory();
            //battleShipFactory.Create(new Health(100.0f, 100.0f));
        }
    }
}
